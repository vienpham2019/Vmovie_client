const inputTypeEnum = Object.freeze({
  TEXT_AREA: "text-area",
  INPUT: "input",
  SELECT: "select",
  INPUT_LIST: "input-list",
  FILE: "file",
  VIDEO: "video",
});

const inputValidateEnum = Object.freeze({
  VALID: "valid",
  INVALID: "invalid",
});

export { inputTypeEnum, inputValidateEnum };
